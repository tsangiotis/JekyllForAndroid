<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gr.tsagi.jekyllforandroid.app" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.LoginActivity"
            android:label="Login"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".ui.PostsActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.EditPostActivity"
            android:label="@string/action_newpost" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".app.activities.PostsListActivity" />
        </activity>
        <activity
            android:name=".ui.PreviewMarkdownActivity"
            android:label="@string/title_activity_markdown" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="app.activities.EditPostActivity" />
        </activity>

        <provider
            android:name="gr.tsagi.jekyllforandroid.app.provider.PostsProvider"
            android:authorities="@string/content_authority"
            android:exported="false"
            android:syncable="true" />

        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
    </application>

</manifest>